# Profile PROD (comentado): MySQL (RDS)
# Dejamos este archivo solo como referencia, no se usa en dev.
# Para activarlo, descomentar y ejecutar con --spring.profiles.active=prod

# spring.datasource.url=jdbc:mysql://<host>:3306/catalogue_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=<user>
# spring.datasource.password=<password>
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always
# spring.sql.init.data-locations=classpath:data.sql
